version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
  mcp_server:
    build: ./backend/mcp_server
    ports:
      - "3001:3001"
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
